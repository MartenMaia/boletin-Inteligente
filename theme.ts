import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    primary: {
      main: '#49708a'
    },
    secondary: {
      main: '#88abc2'
    },
    background: {
      default: '#ebf7f8'
    }
  }
})

export default theme
